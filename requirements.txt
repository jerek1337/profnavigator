# Зависимости проекта «ПрофНавигатор FinTech»
#
# Обязательных зависимостей нет: вся логика, HTTP-клиент и веб-сервер написаны
# на стандартной библиотеке Python 3.10+ (asyncio, urllib, http.server, sqlite3,
# ssl, json, dataclasses). Это сделано намеренно — сервис поднимается на любой
# машине с Python, без доступа к индексу пакетов.
#
# Ниже — единственная необязательная библиотека. Без неё бот работает полностью,
# перестаёт отдаваться только QR-код на плакате (/qr.svg, /poster).
#
#   qrcode  — генерация QR-кода, лицензия BSD, https://pypi.org/project/qrcode/

qrcode>=7.4,<9
